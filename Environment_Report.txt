LINUX ENVIRONMENT VERIFICATION REPORT

1. Username and Groups
Command: whoami → coder
Command: groups → coder sudo
Observation: The user is "coder" and belongs to two groups: "coder" (personal group) and "sudo" (allows admin/root access via sudo).

2. Current Shell
Command: echo $SHELL → /bin/bash
Observation: The system is using the Bash shell, confirmed by the $SHELL variable.

3. Current Working Directory
Command: pwd → /home/coder
Observation: The session starts in the user's home directory, /home/coder, which is standard after login.

4. Files and Directories in Workspace
Command: ls
Output: coursera, data, Desktop, Desktop-ro, Desktop.save, Documents, Downloads, install, Music, node_modules, noVNC, package.json, package-lock.json, Pictures, Public, Templates, Videos, wm.log, wm_startup.sh, workspace, wrapper_process.sh
Observation: The home directory contains standard user folders (Desktop, Documents, Downloads, etc.) plus project files like package.json, node_modules, and shell scripts, along with a dedicated "workspace" folder for project work.

5. Network Connectivity Verification
Command: ping -c 3 google.com
Output: PING resolved google.com to 192.178.155.100, but all 3 packets returned "Destination Port Unreachable" from the internal gateway (172.18.0.1), resulting in 100% packet loss.
Observation: DNS resolution worked correctly, showing the environment can look up hostnames. However, outbound connectivity is blocked, likely due to sandbox/firewall restrictions in this containerized environment. This confirms the workspace has limited or no external internet access.

END OF REPORT
